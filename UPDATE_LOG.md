# 更新ログ

サイトや関係者向け機能の変更内容を、**デプロイ単位で日付順**（新しい日付が上）に残します。  
変更を入れた人は、該当日付の見出しに箇条書きで追記するか、新しい日付の見出しを追加してください。コミットハッシュを括弧に入れると後から追いやすくなります。

---

## 2026-05-15

- 応援メッセージフォームを折りたたみ式に変更。「メッセージを頂ける方はこちら」クリックで展開（`814875c`）
- 応援メッセージ・ヘッダー／フッターのブランド名を英語ページ対応（`577d9fb`）
- 英語ページの表記を一括修正：Miyazaki SELENE に統一・（セレーネ）削除・スタッフ名 Hisadome 修正・役職順修正（`1e34e4b`）
- 応援メッセージ投稿・管理機能を追加。Supabase `messages` テーブル・公開フォーム・関係者管理画面（承認・削除）（`61a2e13`）
- 日本語・英語の多言語対応（next-intl）を実装。URL 構造 `/`（JA）/ `/en/`（EN）、hreflang、言語切替ボタン（`6d15495`）

## 2026-05-14

- SNS シェア後 3 時間の Vercel Analytics データ（68訪問者・549 PV・43%バウンス）を記録（`d9b5e54`）
- Analytics PDF 自動取り込みスクリプト（`scripts/ingest-analytics.mjs`）を追加（`c136806`）
- Instagram スマホ版の説明文を削除（`1ba6af2`）
- 関連リンクをトランプ形式から折りたたみ式（お知らせ・活動記録と同スタイル）に変更（`c4d0744`）
- サイト内検索（⌘K スポットライト型 CommandDialog）を追加。お知らせ・活動記録・FAQ・各セクションを横断検索（`beead78`）
- スマホメニューのハッシュリンクが反応しない不具合を修正（Radix Sheet のスクロールロック競合対策）（`1d62378`）
- 活動記録をお知らせと同じ折りたたみ式リストに変更（`505f7a5`）
- お知らせ一覧に「閉じる」ボタン追加・お問い合わせの募集終了カード削除・関連リンク説明文修正（`5aac29a`）
- FAQ 回答文（練習場所・募集・大会）を更新（`eb31e21`）
- Supabase 読み取り失敗時の JSON フォールバックと SITE_URL の安全化（`4fd9c21`）
- 各 Supabase 連携データの読み書きを整備（`1fe282d`）

## 2026-05-13

- お知らせ・活動記録の詳細ページ、サイトマップ、robots.txt を追加。SEO 強化（`2975bed`〜`3f74860`）
- Google Search Console 所有権確認タグを追加（`a00a277`）
- 活動記録の管理ページを追加。開始日・終了日・複数対戦相手の入力に対応（`71a4bdc`〜`1e889fd`）
- お知らせ管理に修正・削除機能を追加し、日付入力を開始日・終了日に統一（`b739a7b`〜`87e0f5d`）
- お知らせの自動アーカイブ（終了日翌日に「過去のお知らせ」へ移動）を実装（`fa36ff6`）
- SEO: FAQPage・BreadcrumbList・SportsEvent JSON-LD 構造化データを追加（`8b781f6`）
- 関係者ページのセッション修復処理を追加（`a84f898`）
- チーム紹介「私たちの目的」装飾を調整（`38752a8`）

## 2026-05-12

- チーム紹介ギャラリーの画像管理（アップロード・削除・説明編集）を整備（`55f83bb`〜`6820373`）
- スマホ表示のコンパクト化、スタッフナビのレイアウト調整（`c32ff8b`）
- 活動記録アコーディオンを復元（`1b292ec`）

## 2026-05-11

- Analytics JSON を複数スナップショット対応にし、前回比・累積 PV・ページ別伸びを表と Recharts グラフで表示（`stats` 推奨）（`5e86de1`）
- 関係者ダッシュボードを PDF から読み取った数値の日本語表に刷新し、外部サイト案内・PDF 埋め込みを削除。Instagram は「フォロー」ボタンをプロフィールと同じボックス内に集約
- `UPDATE_LOG.md` を新設し、以降の更新記録をここに残す運用を開始。関係者向けに `/staff/update-log` から閲覧できるようにし、ナビ・認証後メニューから辿れるようにした（`6147a89`）
- 関係者ダッシュボードに Vercel Analytics の PDF（`website – Analytics – Vercel`）を `public/reports` に同梱し、iframe 表示・ダウンロード導線を追加（`3060eb2`）
- 同ダッシュボードに、PDF と併せて編集可能な JSON サマリー枠を追加（`271e25d`）
- Vercel Speed Insights を導入（`94ded01`）
- 関係者ダッシュボードの Analytics 案内 UI を整理（`b168d53`）
- お知らせに 5/30 大宮中学練習試合・6/20–21 U15 夏季大会を追加（`da80c20`）
- 関係者専用を認証後メニューと `/staff/results`・`/staff/dashboard` に分割（`598f2d8`）
- `tsconfig.tsbuildinfo` を Git 追跡対象外に（`1efd2ff`）
- 関係者ページに Vercel Analytics への案内を追加（`9f4492b`）

## 2026-05-10

- 試合動画 URL の保存ボタンを常時表示に戻す（`b6dda31`）
- 関係者ページの試合データを API 経由でサーバー保存し端末間で同期（`b5ac99d`）
- チーム紹介ギャラリーに写真追加・タイトル修正・表示方式の調整（`d83a1bc` ほか）
- お知らせに 5/31 練習試合予定を追加（`9ee8593`）
- Instagram 周りの導線・文言の整理（`79901bc` ほか）
- お問い合わせからメール導線を削除（`f639de7`）
- 関係者ページの見出し・試合動画 UI の調整（`4f35556` ほか）
- Instagram 埋め込み・活動記録 UI・型定義まわりの更新（`d004a5c` ほか）
